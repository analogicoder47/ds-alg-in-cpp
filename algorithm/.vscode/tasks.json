{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "echo command",
            "type": "shell",
            "command": "echo",
            "args": [
                "${workspaceFolder}"
            ]
        },
        {
            "label": "build selection_sort",
            "type": "shell",
            "command": "g++",
            "args": [
                "-o",
                "${workspaceFolder}/bin/selection_sort",
                "${workspaceFolder}/src/sort/selection_sort.cpp"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        },
        {
            "label": "build binary_search",
            "type": "shell",
            "command": "g++",
            "args": [
                "-o",
                "${workspaceFolder}/bin/binary_search",
                "${workspaceFolder}/src/tree/binary_search.cpp"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        },
        {
            "label": "build quicksort",
            "type": "shell",
            "command": "g++",
            "args": [
                "-o",
                "${workspaceFolder}/bin/quicksort",
                "${workspaceFolder}/src/sort/quicksort.cpp"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}